using IHM.Model;
using Newtonsoft.Json;
using System;
using System.Collections.Generic;
using System.Configuration;
using System.IO;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace IHM.Helpers
{
    class Functions
    {
        /*public List<T> JSONObject(string str)
        {
            List<Base> items;
            try
            {
                StreamReader r;
                using (r = new StreamReader(@ConfigurationSettings.AppSettings[str]))
                {
                    string json = r.ReadToEnd();
                    items = JsonConvert.DeserializeObject<List<T>>(json);
                }
            }
            catch (Exception)
            {
                items = new List<T>();
            }

            return items;
        }*/
    }
}
