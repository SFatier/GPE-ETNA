<Window x:Class="IHM.View.PopUp"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IHM.View"
        mc:Ignorable="d"
        Title="" Height="300" Width="300">
    <Grid>
        <ListBox HorizontalAlignment="Left" Height="219" Margin="8,41,0,0" VerticalAlignment="Top" Width="274" ItemsSource="{Binding LstProjet}">
            <ListBox.ItemTemplate >
                <DataTemplate>
                    <StackPanel Orientation="Vertical" >
                        <CheckBox Content="{Binding Nom}"  IsChecked="{Binding Ischecked, Mode=TwoWay}"/>                   
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBox HorizontalAlignment="Left" Height="26" Margin="11,10,0,0" TextWrapping="Wrap" Text="Rechercher ..." VerticalAlignment="Top" Width="234"/>
        <Button Content="F" HorizontalAlignment="Left" Margin="249,10,0,0" VerticalAlignment="Top" Width="34" Height="26"/>

    </Grid>
</Window>
