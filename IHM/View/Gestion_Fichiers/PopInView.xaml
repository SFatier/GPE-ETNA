<UserControl x:Class="IHM.View.Gestion_Fichiers.PopInView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IHM.View.Gestion_Fichiers"
             mc:Ignorable="d" >
   
    <Grid Background="Transparent">
        <ListBox HorizontalAlignment="Left" Height="470" VerticalAlignment="Top" Width="226" ItemsSource="{Binding LstProjet}">
            <ListBox.ItemTemplate >
                <DataTemplate>
                    <StackPanel Orientation="Vertical" >
                        <CheckBox Content="{Binding Nom}"  IsChecked="{Binding Ischecked, Mode=TwoWay}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <Button Content="Fermer " Command="{Binding QuitCommand}"  HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10" />
    </Grid>
</UserControl>
